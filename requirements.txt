# 기존 requirements.txt에 추가할 의존성들

# 웹 프레임워크
fastapi>=0.104.0
uvicorn[standard]>=0.24.0

# HTTP 클라이언트 (콜백 기능용)
aiohttp>=3.9.0
requests>=2.31.0

# 웹 스크래핑
beautifulsoup4>=4.12.0
lxml>=4.9.0

# 데이터 처리
pandas>=2.1.0
numpy>=1.24.0

# PDF 처리 (선택사항)
PyMuPDF>=1.23.0

# 이미지 처리 (선택사항)
Pillow>=10.0.0
matplotlib>=3.8.0

# Google AI
google-generativeai>=0.3.0

# Windows HWP 처리 (Windows 전용)
pywin32>=306; sys_platform == "win32"

# 기타 유틸리티
python-multipart>=0.0.6
python-dotenv>=1.0.0
cloudinary>=1.36.0

